#include "jaqc_admin.h"
#include "models.h"
#include "model_config.h"

esp_err_t factory_reset() {

    // cfg_defaults(&s_cfg);
    // opp_defaults(&s_opp);
    // ops_defaults(&s_ops);
    // comm_defaults(&s_comm);
    
    return ESP_OK;
}
